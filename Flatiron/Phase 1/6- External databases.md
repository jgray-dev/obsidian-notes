Git is a version control system used for tracking changes in code repositories. GitHub is a popular hosting service for Git repositories.

`git init git add .` 
`git commit -m "Initial commit"`
`git push origin main`

