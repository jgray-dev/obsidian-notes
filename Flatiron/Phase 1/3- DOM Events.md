You can listen for and respond to events on DOM elements, such as clicks, key presses, and more.

```
const button = document.querySelector('button'); button.addEventListener('click', () => { console.log('Button clicked!'); });
```
